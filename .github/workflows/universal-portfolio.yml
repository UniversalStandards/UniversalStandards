name: Universal Portfolio and Agent Loop

on:
  issues:
    types: [opened, reopened, edited, closed]
  pull_request_target:
    types: [opened, synchronize, reopened, edited, ready_for_review, converted_to_draft, closed]
  issue_comment:
    types: [created]
  pull_request_review:
    types: [submitted]
  pull_request_review_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      mode:
        description: Reconciliation mode
        required: true
        default: open-items
        type: choice
        options:
          - open-items
          - all-items
          - labels-only
          - full-reconcile
      dry_run:
        description: Preview reconciliation without mutations
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  issues: write
  pull-requests: write
  checks: read
  statuses: read

jobs:
  universal-portfolio:
    uses: Universal-Standard/.github/.github/workflows/universal-portfolio-agent.yml@9ce58c231e7fdb71b4630c536936b3e41dae90f8
    with:
      system: universal-standards
      policy_ref: 9ce58c231e7fdb71b4630c536936b3e41dae90f8
      project_id: ${{ vars.PORTFOLIO_PROJECT_ID || '' }}
      enable_auto_merge: true
      max_remediation_passes: 10
      reconcile_mode: ${{ github.event_name == 'workflow_dispatch' && inputs.mode || 'current-item' }}
      dry_run: ${{ github.event_name != 'workflow_dispatch' || inputs.dry_run }}
    secrets:
      COPILOT_AGENT_TOKEN: ${{ secrets.COPILOT_AGENT_TOKEN }}
      PROJECTS_TOKEN: ${{ secrets.PROJECTS_TOKEN }}
